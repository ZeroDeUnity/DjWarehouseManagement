<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>杭州东骏库存管理 - 登录</title>
    <!-- 引入BootSharp和Bootstrap的CSS -->
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <style>
        /* 固定头部和按钮区域 */
        body,
        html {
            height: 100%;
            margin: 0;
            padding: 0;
        }

        .fixed-header {
            position: fixed;
            top: 0;
            width: 100%;
            background-color: #f8f9fa;
            z-index: 1000;
            padding: 10px 0;
        }

        .buttons-section {
            position: fixed;
            top: 60px;
            width: 100%;
            background-color: #f8f9fa;
            z-index: 1000;
            padding: 10px 0;
        }

        .content-section {
            margin-top: 80px;
            overflow-y: auto;
            height: calc(100% - 140px);
            padding: 10px;
        }

        /* 登录表单样式 */
        .login-form {
            width: 300px;
            margin: 0 auto;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

            .login-form input {
                width: 100%;
                margin-bottom: 15px;
                padding: 10px;
                border: 1px solid #ccc;
                border-radius: 3px;
            }

            .login-form button {
                width: 100%;
                padding: 10px;
                background-color: #007bff;
                color: #fff;
                border: none;
                border-radius: 3px;
            }
    </style>
</head>

<body>
    <!-- 第一部分：标题 -->
    <div class="fixed-header text-center">
        <h2>杭州东骏库存管理</h2>
    </div>

    <!-- 第二部分：按钮（可后续添加与登录相关的按钮等操作） -->
    <div class="buttons-section text-center">

    </div>

    <!-- 第三部分：内容展示区，放置登录表单 -->
    <div class="content-section" id="contentArea">
        <form class="login-form">
            <input type="text" id="username" placeholder="用户名" />
            <input type="password" id="password" placeholder="密码" />
            <button type="button" id="loginBtn">登录</button>
        </form>
    </div>

    <!-- 引入jQuery和Bootstrap的JS -->
    <script src="~/Scripts/jquery-3.7.0.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

    <script>
        $(document).ready(function () {
            // 点击登录按钮的处理逻辑，这里只是简单模拟验证，实际需对接后端
            $('#loginBtn').click(function () {
                var username = $('#username').val();
                var password = $('#password').val();
                if (username === "" || password === "") {
                    return;
                }

                //用ajax请求后端验证,如果返回成功则跳转到首页
                $.ajax({
                    url: '/Login/UserLogin',
                    type: 'POST',
                    data: {
                        username: username,
                        password: password
                    },
                    success: function (data) {
                        if (data === "true") {
                            window.location.href = '/Home/Index';
                        } else {
                            alert('用户名或密码错误');
                        }
                    }
                });

            });
        });
    </script>
</body>

</html>